#include <iostream>
#include<stdlib.h>
#include<math.h>
/*
typedef struct pyr pyr_t;
struct pyr {
	int n;
	int **levels;
};
pyr_t* alloc_pyr(int n) { // assume n > 0
	pyr_t* p = malloc(sizeof(pyr_t));
	p->n = n;
	p->levels = malloc(n * sizeof(int*));
	for (int k = 0; k < n; k++) {
		p->levels[k] = malloc((k + 1) * sizeof(int));
	}
	return p;
}
void free_pyr(pyr_t *p)
{
	for (int k = 0; k < p->n; k++) {
		free(p->levels[k]);
	}
	free(p->levels);
	free(p);

}

*/

void main()
{

	
}